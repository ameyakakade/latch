package com.vinnovateit.latch.ui.theme

import androidx.compose.ui.graphics.Color
import com.materialkolor.dynamicColorScheme

fun testCompile() {
    val x = dynamicColorScheme(seedColor = Color.Red, isDark = true)
}
